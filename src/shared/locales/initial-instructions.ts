import type { SupportedLocale } from "./locale.js";

const initialInstructionsByLocale: Record<SupportedLocale, string[]> = {
  ja: [
    "## 常用ルール",
    "- 常に日本語で返答する。",
    "- まず `context_compile` を呼び、作業の主導線とする。`goal` を必ず渡し、適切な `changeTypes` / `technologies` を指定する。",
    "- `goal` は達成したい状態を1-3文で具体的に書き、設計書パスや `design.md` などの文書参照は含めない。",
    "- ユーザーに情報を提示する際、それが本当に有用であるかを厳格に評価し、不確実な情報やノイズでコンテキストを圧迫しない。",
    "- 作業中に再利用可能なルールや手順（手続き）を確立した場合は、`register_candidate`（複数なら `register_candidates`）で即時登録する。",
    "- candidate 登録時は、汎用的に使える知識として体裁を整える。",
    '- 失敗経験や修正手順は、次の JSON 形に整えて登録する: `{ "title": "...", "type": "procedure", "body": "Use when:\\n- ...\\n\\nWorkflow:\\n1. ...\\n2. ...\\n\\nVerification:\\n- ...\\n\\nAvoid:\\n- ...", "technologies": ["..."], "changeTypes": ["..."], "domains": ["..."] }`',
    "- 完了報告の前に、`context_compile` の実行回数と `compile_eval` の実行回数を自己申告する。また、各 runId ごとに `compile_eval` を1件以上保存する。",
    "- `session_memo` は採点ではなく短期メモ用途で使う。",
    "",
    "## hooksLLM (LLM Hooks / `hooks.json`) の活用指針",
    "- 定型的・自動実行可能なチェックは、Antigravity の `hooks.json` や Git Hooks（`hooksLLM`）へ積極的に委ね、手動チェックの負荷とエラーを最小化するようユーザーに推奨する。",
    "- 【PreToolUse】ファイル書き込み `write_file`/`replace_file_content` の直前に、セキュリティスキャン（シークレット検知）や規約バリデーションを走らせるよう hooks を設定・活用する。",
    "- 【PostToolUse】コード生成・変更の直後に `linter` や自動テストを実行し、フィードバックを即座に受けるようにする。",
    "- 【post-commit】コミット完了時に、コミットから再利用可能な知見や手順を自動で抽出し `register_candidate` を促す Git Hooks（`post-commit-candidate-reminder`）を活用する。",
    "",
    "## MCPツール種別",
    "- `context_compile`: 作業前の最小コンテキスト生成（主導線）。",
    "- `register_candidate`: 新しいルールや手順（スキル）候補の即時登録。",
    "- `register_candidates`: 複数 candidate の一括登録。",
    "- `compile_eval`: `context_compile` の作業後評価を保存。",
    "- `session_memo`: session単位の一時メモ保存・参照。",
    "- `search_memory` / `fetch_memory`: 過去会話・差分の参照（補助）。",
    "- `doctor`: DB / embedding / automation / run health の診断。",
  ],
  en: [
    "## Operational Rules",
    "- Always respond in Japanese.",
    "- First call `context_compile` as the main baseline of the task. Always provide `goal`, and specify appropriate `changeTypes` / `technologies`.",
    "- Keep the `goal` focused on 1-3 specific sentences describing the desired outcome. Do not include path references like `design.md` or implementation plans.",
    "- Strictly evaluate if the presented information to the user is truly useful and specific to avoid context pollution.",
    "- If reusable rules, guidelines, or procedures are established, immediately register them using `register_candidate` (or `register_candidates` for multiple).",
    "- When registering candidates, shape them as generally usable knowledge.",
    '- Format failure patterns and workflow lessons into JSON before registering: `{ "title": "...", "type": "procedure", "body": "Use when:\\n- ...\\n\\nWorkflow:\\n1. ...\\n2. ...\\n\\nVerification:\\n- ...\\n\\nAvoid:\\n- ...", "technologies": ["..."], "changeTypes": ["..."], "domains": ["..."] }`',
    "- Before announcing completion, self-report the count of `context_compile` and `compile_eval` executions. Record at least one `compile_eval` for each runId in the session.",
    "- Use `session_memo` for short-term session notes rather than quality evaluation.",
    "",
    "## hooksLLM (LLM Hooks / `hooks.json`) Guidelines",
    "- Actively delegate repetitive or automated verification to Antigravity's `hooks.json` or Git Hooks (`hooksLLM`) to minimize manual checking overhead and errors, recommending users to set them up.",
    "- [PreToolUse] Configure hooks to run security scanning (secret detection) or code style validation right before tools like `write_file` or `replace_file_content` are executed.",
    "- [PostToolUse] Automatically run linters or test suites right after code modifications to catch degradation immediately.",
    "- [post-commit] Utilize git post-commit hooks (like `post-commit-candidate-reminder`) to automatically analyze changes and prompt for `register_candidate` when reusable lessons are committed.",
    "",
    "## MCP Tool Reference",
    "- `context_compile`: Generates the baseline minimal context before working.",
    "- `register_candidate`: Registers a single new rule or procedure candidate.",
    "- `register_candidates`: Batch registers multiple candidates.",
    "- `compile_eval`: Saves post-task evaluation metrics for `context_compile` runs.",
    "- `session_memo`: Short-term session memo storage and retrieval.",
    "- `search_memory` / `fetch_memory`: Reference past conversations or diff history.",
    "- `doctor`: Diagnostic checkup for DB, embedding, and automation health.",
  ],
};

export function buildInitialInstructionsText(locale: SupportedLocale): string {
  return initialInstructionsByLocale[locale].join("\n");
}
