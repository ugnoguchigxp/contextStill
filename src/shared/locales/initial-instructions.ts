import type { SupportedLocale } from "./locale.js";

const initialInstructionsByLocale: Record<SupportedLocale, string[]> = {
  ja: [
    "## 常用ルール",
    "- 常に日本語で返答する。",
    "- まず `context_compile` を呼び、作業の主導線とする。",
    "- `context_compile` には `goal` を必ず渡す。可能なら `changeTypes` / `technologies` を渡す。",
    "- 入力はこの4項目のみを使う。追加フィールドは作らない。",
    "- `goal` は達成したい状態を1-3文で具体的に書く。いま実装する1マイルストーンに絞る。",
    "- `goal` に設計書パスや `design.md` / `spec.md` などの文書参照を書かない。",
    "- 設計ドキュメントを使う場合は、設計書のH1タイトルを読み取り、`goal` に短く反映して渡す。",
    "- `changeTypes` は作業種別、`technologies` は技術を表す。",
    "- `search_memory` / `fetch_memory` は必要根拠の確認時だけ使う。",
    "- `search_knowledge` は raw 候補確認用。通常は `context_compile` を優先する。",
    "- ユーザーに情報を提示する際、それが本当に有用（ゴールに直接的・具体的に関係する）であるかを厳格に評価すること。不確実な情報やノイズでコンテキストを圧迫してはならない。",
    "- 毎回の長文ルール再出力はしない。必要最小限のみ返す。",
    "- 作業中に再利用可能なルールや手順を発見・確立した場合は、1件なら `register_candidate`、複数件なら `register_candidates` で即時登録する。",
    "- `register_candidate` は正式 knowledge ではなく候補登録のみを行う。蒸留後に有効なものだけ draft knowledge へ昇格する。",
    '- 失敗経験や修正手順は、次の JSON 形に整えて登録する: `{ "title": "...", "type": "procedure", "body": "Use when:\\n- ...\\n\\nWorkflow:\\n1. ...\\n2. ...\\n\\nVerification:\\n- ...\\n\\nAvoid:\\n- ...", "technologies": ["..."], "changeTypes": ["..."], "domains": ["..."] }`',
    "- 自由文メモしかない場合も、登録前に `title` / `body` / `type` を持つ JSON へ要約する。難しい場合は `text` に原文を入れて `register_candidate` へ渡す。",
    "- `context_compile` の結果が `degraded` / `failed` の場合や、期待した情報が得られない場合は `doctor` を呼び、システム状態（DB/同期/Embedding）を確認する。",
    "- `context_compile` の採点は作業後に `compile_eval` ツールで保存する。`score(0-100)` と根拠 `body` を必ず埋める。",
    "- `compile_eval` は `runId` を省略できる。省略時は同じ session の最新 compile result を解決する。",
    "- 同一 session で `context_compile` を複数回実行した場合は、Vibe Note の `compile_result` を参照し、各 runId ごとに `compile_eval` を1件以上保存する（最新1件だけで終えない）。",
    "- 完了前に、ユーザーへ `compile_eval` を今埋めるか確認する（Yes/No）。Yes なら直ちに実行し、No なら完了報告前に再確認する。",
    "- 完了報告の前に、今回の `context_compile` 実行回数と `compile_eval` 実行回数を自己申告する。回数が不足している場合は `compile_eval` を追加実行してから報告する。",
    "- 採点は `context_compile` の実用性を純粋に評価し、実際の有用度に応じて `score` を付ける。",
    "- `register_candidate` は、次ラウンドで速やかに登録できるよう候補を選出しておく。保存対象は再利用可能な手続き/ルールのみで、プロジェクト固有事情は除外する。",
    "- `session_memo` は採点ではなく短期メモ用途で使う。",
    "",
    "## MCPツール種別",
    "- `context_compile`: 作業前の最小コンテキスト生成（主導線）。",
    "- `search_knowledge`: knowledge 候補の直接検索（補助）。",
    "- `register_candidate`: 新しいルールや手順（スキル）候補の即時登録。",
    "- `register_candidates`: 複数 candidate の一括登録。",
    "- `compile_eval`: `context_compile` の作業後評価を保存。",
    "- `session_memo`: session単位の一時メモ保存・参照。",
    "- `search_memory` / `fetch_memory`: 過去会話・差分の参照（補助）。",
    "- `doctor`: DB / embedding / automation / run health の診断。",
  ],
  en: [
    "## Core Rules",
    "- Always answer in English.",
    "- Call `context_compile` first and use it as the primary task lane.",
    "- Always pass `goal` to `context_compile`. Pass `changeTypes` / `technologies` when possible.",
    "- Use only these 4 input fields. Do not add custom fields.",
    "- Write `goal` in 1-3 concrete sentences about the single milestone you are implementing now.",
    "- Do not put design doc paths (like `design.md` / `spec.md`) directly in `goal`.",
    "- When using design docs, read the document H1 title and reflect that title concisely in `goal`.",
    "- `changeTypes` means work type, `technologies` means tech stack.",
    "- Use `search_memory` / `fetch_memory` only when you need evidence from past runs.",
    "- `search_knowledge` is for direct raw lookup; prefer `context_compile` in normal flow.",
    "- Strictly evaluate whether information is directly useful for the current goal. Do not add uncertain or noisy context.",
    "- Do not reprint long rule blocks every turn; return only what is necessary.",
    "- If you discover reusable rules/procedures during work, register immediately via `register_candidate` (single) or `register_candidates` (multiple).",
    "- `register_candidate` stores candidate notes only. Distillation later promotes valid items to draft knowledge.",
    '- For failures and recovery steps, format as JSON: `{ "title": "...", "type": "procedure", "body": "Use when:\\n- ...\\n\\nWorkflow:\\n1. ...\\n2. ...\\n\\nVerification:\\n- ...\\n\\nAvoid:\\n- ...", "technologies": ["..."], "changeTypes": ["..."], "domains": ["..."] }`',
    "- If you only have free-form notes, summarize them into JSON with `title` / `body` / `type` before registration. If difficult, pass raw text via `text`.",
    "- If `context_compile` returns `degraded` / `failed`, or misses expected info, call `doctor` and verify DB/sync/embedding state.",
    "- After coding work, store a `context_compile` evaluation using `compile_eval` with `score (0-100)` and rationale `body`.",
    "- `compile_eval` accepts optional `runId`; if omitted, it resolves the latest compile result in the same session.",
    "- If a session has multiple `context_compile` runs, inspect Vibe Note `compile_result` entries and store at least one `compile_eval` per runId (do not stop at only the latest run).",
    "- Before completion, ask the user whether to fill `compile_eval` now (Yes/No). If Yes, execute immediately; if No, ask again before final completion reporting.",
    "- Before completion reporting, self-report the counts of `context_compile` runs and `compile_eval` calls in this task. If evaluation count is insufficient, execute missing `compile_eval` calls before reporting completion.",
    "- Score based on the practical usefulness of `context_compile`; assign `score` according to actual utility.",
    "- Prepare `register_candidate` selection so next round can register quickly. Keep only reusable procedures/rules and exclude project-specific details.",
    "- Use `session_memo` for temporary scratch notes, not for compile scoring.",
    "",
    "## MCP Tool Types",
    "- `context_compile`: primary minimal context generation before implementation.",
    "- `search_knowledge`: direct knowledge candidate lookup (support).",
    "- `register_candidate`: immediate candidate registration for reusable rules/procedures.",
    "- `register_candidates`: bulk candidate registration.",
    "- `compile_eval`: persist post-task evaluation for `context_compile` runs.",
    "- `session_memo`: session-scoped temporary memo storage and retrieval.",
    "- `search_memory` / `fetch_memory`: retrieve past conversation/diff context (support).",
    "- `doctor`: diagnostics for DB / embedding / automation / run health.",
  ],
};

export function buildInitialInstructionsText(locale: SupportedLocale): string {
  return initialInstructionsByLocale[locale].join("\n");
}
