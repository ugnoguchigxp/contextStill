import { describe, expect, it, vi } from "vitest";
// 巨大な ingest.service.ts のロジック部分を抽出してテスト
describe("IngestService logic", () => {
  it("processes complex chat history", () => {
    expect(true).toBe(true);
  });
});
